- class=틀  -> 산물 =instance  (생성한다 라는 뜻의 키워드는 new)

- 만든다, ~~한다 -> 기능(변수,함수)
변수는 명사에대한 것 / 함수는 동사에 대한것
생성한다는 동사로 함수에 포함됨 new 생성함수(생성자)

- new 생성자(       );
생성함수(생성자)의 규칙  
클래스의 이름과 동일해야한다. 
public class A {
	//A(사용자 정의 자료형) a(변수명) = new A();
}
a 는 주소값을 가지고 있는 참조변수

-클래스의 구성요소는 
1. 변수(필드, field, 멤버변수)
2. 함수(멤버함수, 메소드, method)
클래스 다이어 그램

A                - 클래스이름
_______________

n : int
		-변수 목록

________________
f(int); : void		
		-함수목록


*함수의 시그니처(함수이름, 매개변수, 함수의타입)



